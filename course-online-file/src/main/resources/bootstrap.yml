spring:
  cloud:
    nacos:
      discovery:
        # nacos地址
        server-addr: 127.0.0.1:8848
      config:
        # config 多配置文件
        extension-configs:
          - data-id: jackson-dev.yml
            refresh: true
          - data-id: aliyun.yml
            refresh: true